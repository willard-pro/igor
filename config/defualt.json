{
	"required": {
		"commands": [
			{
				"command": "awk",
				"message": "${RED}Error:${YELLOW} 'awk' command not found. Please install awk and try again${RESET}."
			},
			{
				"command": "gh",
				"message": "${RED}Error:${YELLOW} 'gh' command not found. Please install GitHub CLI (https://docs.github.com/en/github-cli/github-cli/quickstart) and try again${RESET}."
			},
			{
				"command": "git",
				"message": "${RED}Error:${YELLOW} 'git' command not found. Please install git and try again${RESET}."
			},
			{
				"command": "sed",
				"message": "${RED}Error:${YELLOW} 'sed' command not found. Please install sed and try again${RESET}."
			}
		]
	}
}
